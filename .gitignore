# Editor/OS files
.vscode/
.DS_Store
Thumbs.db

# Dependency/build folders
node_modules/
dist/
build/

# Logs
*.log

# BC Drop — local staging folder for BC exports (not committed)
bc_drop/

# Security review — confidential; repo is public (jsDelivr), keep out of git
SECURITY_REVIEW.md
SECURITY_RESPONSE.md
POWERBI_DATABRICKS_GAP.md

# Live DB schema snapshot — full structure dump; repo is public, so ignored by
# default until we decide deliberately whether structure disclosure is fine.
core/sql/_schema_snapshot.sql
core/sql/_audit_output/

# Supabase CLI working dir. `supabase link` creates supabase/.temp with project
# credentials/state. The CLI cannot reach the DB from the office network anyway
# (ports 5432/6543 are blocked, 443 is not) — see core/sql/_dump_definitions.sql.
supabase/
